# Chore: <CHORE_NAME>

## Description

<What needs to be done and why it matters>

## Current State

<What exists today that needs to change — specific files, structure, behavior>

## Desired State

<What it should look like after — specific files, structure, behavior>

## Relevant Files

- `path/to/file.py` - <what changes and why>
- `path/to/directory/` - <what changes and why>

## Implementation Plan

### Relevant Patterns
<Codebase patterns that implementation tasks should follow>
<Each entry references a concrete file path + line range>

- `path/to/file.py:L42-L80` — <Pattern name>: <How tasks should follow this pattern>
- `path/to/other.py:L10-L35` — <Pattern name>: <How tasks should follow this pattern>

### Phase 1: <Phase Name>
<What this phase accomplishes — enough detail to guide bead creation>

### Phase 2: <Phase Name>
<What this phase accomplishes>

## Verification

### How to Verify
<Specific commands to confirm the chore is complete>

```bash
# Example: check the new structure exists
ls -la path/to/expected/structure/
```

### Expected State
- <Concrete observable outcome — file exists, command succeeds, output matches>
- <Concrete observable outcome>

### Regression Checks
- <What existing behavior must not break>
- <Command to verify no regression>

## Acceptance Criteria

- [ ] <Testable criterion with observable outcome>
- [ ] <Testable criterion with observable outcome>
